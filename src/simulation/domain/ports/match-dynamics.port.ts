/**
 * Decision strategy port (what/when events occur).
 * Phase 1 will define: nextStep(sim, tickIndex): Promise<{delayMs, event} | undefined>.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface MatchDynamics {
  // Phase 1 signature
}
